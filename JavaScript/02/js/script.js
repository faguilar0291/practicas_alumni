

let name = window.prompt("Ingresa tu nombre");
console.log(name);

let n = name.slice(0,1);
n = name.toUpperCase();
if (n == "R"){
    name = name + " does not play banjo";
} else {
    name = name + " does not play banjo";
}
return name;